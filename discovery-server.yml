eureka:
  instance:
    hostname: localhost
  # standalone mode
  client:
    registerWithEureka: true
    fetchRegistry: true
# Avoid some debugging logs at startup
logging:
  level:
    org:
      springframework:
        boot: INFO
        web: INFO
